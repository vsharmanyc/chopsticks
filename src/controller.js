let gameState = {
    player1: {
        right: 1,
        left: 1
    },
    player2: {
        right: 1,
        left: 1
    }
}

const makeMoveP1 = (hand) => {
    // attack p2 r
    // attack p2 l
            // ---> By how much

    alert('p1 move made')

    // swap
    // --> don't allow the swap both r and l are the 
}

const makeMoveP2 = () => {
    // attack p2 r
    // attack p2 l
            // ---> By how much
    alert('p2 move made')


    // swap
    // --> don't allow the swap both r and l are the 
}

const checkForWin = () => {
    // is p1 r && l  === 0

    // or is p2 r && l === 0
} 

document.addEventListener('DOMContentLoaded', () => {
    // document.body.innerHTML = JSON.stringify(gameState);
})
